# Webpack Sass loader React Redux
PRODUCTION RUN npm run (or yarn) build
DEVELOPMENT RUN npm run (or yarn) start 
// Single run
$ yarn test

// Watchmode
$ yarn test:watch

npm:

// Single run
$ npm run test

// Watchmode
$ npm run test:watch